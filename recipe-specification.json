{
  "version": 1,
  "plugin_id": "default-agent-0000",
  "plugin_name": "Default agent",
  "plugin_version": 1,
  "supported_resources": [
    {
      "resource_path": {
        "chain_id": "ethereum",
        "protocol_id": "eth",
        "function_id": "transfer",
        "full": "ethereum.eth.transfer"
      },
      "parameter_capabilities": [
        {
          "parameter_name": "amount",
          "supported_types": 1,
          "required": true
        }
      ],
      "required": true,
      "target": 1
    }
  ],
  "requirements": {
    "min_vultisig_version": 1,
    "supported_chains": [
      "Ethereum"
    ]
  },
  "configuration": {
    "properties": {
      "minimumValue": {
        "format": "number",
        "type": "string"
      }
    },
    "required": [
      "minimumValue"
    ],
    "type": "object"
  },
  "permissions": [
    {
      "id": "transaction_signing",
      "label": "Access to transaction signing",
      "description": "The app can initiate transactions to claim rewards from the Merkle distributor"
    },
    {
      "id": "balance_visibility",
      "label": "Vault balance visibility",
      "description": "The app can view Vault balances"
    }
  ]
}